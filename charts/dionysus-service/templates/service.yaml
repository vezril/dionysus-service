apiVersion: v1
kind: Service
metadata:
  name: {{ include "dionysus-service.fullname" . }}
  labels:
    {{- include "dionysus-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dionysus-service.selectorLabels" . | nindent 4 }}
